import { initializeComponents } from "./utils/loadComponents";

window.addEventListener("load", () => initializeComponents(document));

// This is the main JavaScript entry point
// If this file doesn't exist yet, create it with at least some minimal content

// Your JavaScript code goes here
console.log("Site JavaScript loaded");

// Add any other code you need for your site
